apiVersion: batch/v1
kind: CronJob
metadata:
  name: replace-logs
spec:
  schedule: "* * * * *" # Ejecutar cada minut
  concurrencyPolicy: Replace
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: logger
            image: busybox
            command: ["sh", "-c", "echo Replace && sleep 70 && echo Fin"]
          restartPolicy: Never