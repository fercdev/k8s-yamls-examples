apiVersion: v1
kind: Service
metadata:
  name: service-clusterip
  labels:
    app: mi-app
spec:
  type: ClusterIP  # NodePort

  selector:
    app: myapp
  
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80